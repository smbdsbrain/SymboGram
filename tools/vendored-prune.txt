# Paths deleted from the vendored trees after checkout at the pinned commits.
# See docs/VENDORED.md. Nothing here is referenced by libkg.pri, mbedtls.pri,
# headers.pri, zlib.pri or qt-json.pri; those .pri files enumerate every source
# and header the build compiles. No remaining file has been edited.
#
# Reproduce: clone each dependency at its pinned commit, delete these paths.

# --- mbedtls: test suites, sample programs, docs and MSVC/CI scaffolding ---
libkg/mbedtls/tests
libkg/mbedtls/programs
libkg/mbedtls/docs
libkg/mbedtls/doxygen
libkg/mbedtls/visualc
libkg/mbedtls/scripts
libkg/mbedtls/ChangeLog.d
libkg/mbedtls/.github

# --- zlib: contributed bindings, docs, examples and foreign-platform ports ---
libkg/zlib/contrib
libkg/zlib/doc
libkg/zlib/examples
libkg/zlib/test
libkg/zlib/win32
libkg/zlib/msdos
libkg/zlib/old
libkg/zlib/nintendods
libkg/zlib/watcom
libkg/zlib/qnx
libkg/zlib/amiga
libkg/zlib/os400

# --- qt-json: demo application ---
libkg/qt-json/example

# --- tl-generator: demo application, and git metadata for an inlined submodule
# .gitmodules would describe a submodule that is no longer one, and the nested
# .gitignore would apply ignore rules to a subtree of a repository that already
# ignores build output globally -- an ignore rule inside a vendored tree is
# exactly the kind of thing audit check (i) exists to catch.
tools/tl-generator/.gitignore
tools/tl-generator/.gitmodules
tools/tl-generator/qt-json/example
